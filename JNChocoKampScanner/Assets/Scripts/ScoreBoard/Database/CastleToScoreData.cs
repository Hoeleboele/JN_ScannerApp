using System.Collections.Generic;
using UnityEngine;

public class CastleToScoreData : MonoBehaviour
{
    [SerializeField]
    private List<BlockSpawner> spawner = new List<BlockSpawner>();

    private void Update()
    {
        if (Input.GetKeyDown(KeyCode.S))
        {

        }
    }
}
